# 09: Десктоп: тени по событию, чанки, загрузка

**What to build:** на десктопе переход держит не меньше 50 fps, первый экран появляется быстро: тени обновляются по событию с прогревом, three в отдельном чанке, модель и шрифты в preload, critical CSS лоадера инлайн, GTAO только в тёмной сцене.

**Blocked by:** 01 (Каркас E2E)

**Status:** ready-for-agent

- [ ] Shadow map обновляется прогревом на несколько кадров после свапа и затем по событию (изменение пресета, ресайз), не каждый кадр; тени визуально не изменились
- [ ] Vite manualChunks: three и постпроцессинг отдельно от кода страницы; сцена подключается как модуль
- [ ] Preload модели и шрифтов с приоритетом, critical CSS лоадера инлайн в HTML, шрифты с `font-display: swap`
- [ ] Сторонний JS (позже Метрика) подключается по событию первой отрисовки сцены; событие есть
- [ ] E2E desktop: fps во время перехода не ниже 50 в headless Chromium, золотые кадры проходят порог
