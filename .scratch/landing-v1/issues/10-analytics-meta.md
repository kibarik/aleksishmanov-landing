# 10: Метрика и мета-теги

**What to build:** Алекс видит в Яндекс.Метрике цели «дошёл до белой сцены», «нажал Ask», «нажал Написать»; ссылка на сайт в Telegram разворачивается с русским заголовком, описанием и превью-кадром белой сцены.

**Blocked by:** 03 (Кнопка Ask), 04 (Секции после сцены), 07 (Фолбэк без WebGL)

**Status:** done

- [x] Метрика подключается после события первой отрисовки сцены, не блокирует загрузку; идентификатор счётчика в одном месте конфигурации
- [x] Три цели отправляются: белая сцена (по событию таймлайна, один раз за визит), клик Ask (обе кнопки), клик «Написать»
- [x] Title, description, Open Graph (title, description, image = фолбэк-кадр, url), язык `ru`
- [x] E2E: перехват запросов к Метрике подтверждает отправку трёх целей; мета-теги присутствуют

## Comments

- Реализовано: `src/analytics.js` подключает счётчик по событию `scene:first-frame` (без WebGL — сразу, там сцены нет). Цели: `white-scene` (по свапу таймлайна, один раз за визит; в фолбэке шлётся сразу — кадр белой сцены и есть первый экран), `ask` (обе кнопки по id), `contact` (ссылка внутри `#contact`). Идентификатор и имена целей лежат в `content.analytics`.
- Мета: `<title>`, description, Open Graph (type, locale, site_name, title, description, url, image = фолбэк-кадр) и `twitter:card` в `index.html`, `lang="ru"` уже стоял. Абсолютный URL картинки — `https://aleksishmanov.ru/fallback/white-desktop.webp`.
- E2E `analytics-meta.desktop.spec.js`: мета-теги, факт подключения счётчика после prelude и три цели через перехват `window.ym` (в сеть Метрика из тестов не ходит).
- Идентификатор счётчика Яндекс.Метрики: **103355275** (получен от Алекса 22.09.2026). Кладём в одну константу конфигурации, как требует чеклист.
