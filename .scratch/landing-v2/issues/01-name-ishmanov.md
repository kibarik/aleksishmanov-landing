# 01: Имя ISHMANOV на белой сцене

**What to build:** знакомый видит за статуей одно слово «ISHMANOV» вместо «ALEKS ISHMANOV»: буквы крупнее, фигура перекрывает их меньше.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] Имя на белой сцене — «ISHMANOV» на обоих пресетах, из модуля контента
- [x] Ширина имени на десктопе 1440×900 не выходит за кадр, на мобильном 390×844 помещается по ширине
- [x] Золотые кадры белой сцены и фолбэк-кадры перегенерированы
- [x] E2E: имя на странице читается из контента на обоих пресетах

## Comments

- Реализовано: `content.name` теперь `ISHMANOV` на обоих пресетах; подгонка по ширине кадра ужата до 84% (`TITLE_FRAME_FILL`), иначе одно короткое слово растягивалось от края до края.
- Золотые кадры белой сцены и фолбэк-кадры перегенерированы.
